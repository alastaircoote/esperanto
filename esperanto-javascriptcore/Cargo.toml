[package]
edition = "2018"
name = "esperanto_javscriptcore"
version = "0.1.0"

[dependencies]
esperanto_traits = {path = "../esperanto-traits"}
# libc = "0.2.72"
javascriptcore-sys = "0.0.4"
slotmap = "0.4.0"

[build-dependencies]
bindgen = "0.54.1"
cc = {version = "1.0", features = ["parallel"]}
